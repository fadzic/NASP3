$(function() {

    init();

    rbt = new rbTree()

    //draw(bst.toString());
})

function addElement(e){
    rbt = rbt.insert(parseFloat(e))

    mujo={}
    mujo = JSON.stringify(rbt)
    mujo=mujo.replace(/null/gi,"{}")
    console.log(mujo)
    mujo=JSON.parse(mujo)

    draw(mujo)
}

function delElement(e){

    /*bst.remove(e)

    mujo={}
    mujo = JSON.stringify(bst["_root"])
    mujo=mujo.replace(/null/gi,"{}")
    mujo=JSON.parse(mujo)

    //console.log(mujo)
    draw(mujo)*/
}